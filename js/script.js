const adicionar = document.querySelector("#adicionar");
const adicionarInput = document.querySelector("#adicionar-input");

// Função
const salvar = (text)  => {
    
}

// Eventos

adicionar.addEventListener("submit", (e) => {
    e.preventDefault();

    const valorInput = adicionarInput.ariaValueMax;

    if (valorInput) {

    }
});